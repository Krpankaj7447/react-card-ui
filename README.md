 # react-card-ui
 This project is a simple React-based Card UI that display company data dynamically using an array of objects.Each card represents a company and show its details in a structured format.
